import { Recipe as RecipeCardProps } from "@/types/recipe.types";

const RecipeCard: React.FC<RecipeCardProps> = (): JSX.Element => {
    return (
        <div>
            test
        </div>
    );
};

export default RecipeCard;